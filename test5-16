#define _CRT_SECURE_NO_WARNINGS 1
#include<stdio.h>
//写一个数学函数
//int main()
//{
//	int x = 0; 
//	int y = 0;
//	scanf("%d", &x);
//	if (x < 1)
//	{
//		y = x;
//	}
//	else if (x >= 1 && x < 10)
//	{
//		y = 2 * x - 1;
//	}
//	else
//	{
//		y = 3 * x - 11;
//	}
//	printf("y=%d\n", y);
//	return 0;
//}
//求出0～100000之间的所有“水仙花数”并输出。
//“水仙花数”是指一个n位数，其各位数字的n次方之和确好等于该数本身，如 : 153＝1 ^ 3＋5 ^ 3＋3 ^ 3，则153是一个“水仙花数”。
//#include<math.h>
//int main()
//{
//	int i = 0;
//	int sum = 0;
//	for (i = 1; i <= 100000; i++)
//	{
//		int tmp = i;//把i替换为tmp方便最后的打印
//		int count = 1;//一个数最起码也是一位数
//		//判断一个数是几位数
//		while (tmp / 10)
//		{
//			count++;
//			tmp = tmp / 10;
//		}
//		//计算每一位的次方和
//		tmp = i;
//		while (tmp)
//		{
//			sum += pow(tmp % 10, count);//pow其中放的类型是double类型
//			tmp = tmp / 10;
//		}
//		if (sum == i)
//			printf(" %d ",i);
//		sum = 0;//每次循环要把sum重新赋值为0
//	}	
//	return 0;
//}
//写一个代码：打印100~200之间的素数
//#include<math.h>
//int main()
//{
//	int i = 0;
//	int j = 0;
//	for (i = 101; i < 200; i+=2)//素数除了2和3以外，再没有连续在一起的素数
//	{
//		//如果m能被[2,sqrt(m)]之间任意数整除，则m不是素数，原因：一个非素数m，第一个因子肯定小于等于sqrt(m)，最后一个因子必大于sqrt(m)。
//		for (j = 2; j <= sqrt(i); j++)
//		{
//			if (i%j == 0)
//				break;
//		}
//		if (j>sqrt(i))
//			printf("%d\n", i);
//	}
//	return 0;
//}
